# frozen_string_literal: true

RSpec.describe Verify::Firebase::Jwt do
  it "has a version number" do
    expect(Verify::Firebase::Jwt::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
